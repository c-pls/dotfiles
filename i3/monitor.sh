# nums = `xrandr | grep "connected" | wc -l`
# echo nums

# xrandr | grep "disconnected" | wc -l

current_date=$(date)
echo "Today is $current_date"
cur_r=${xrandr}
echo "${x}"

 Display=$(xrandr | grep " connected " | wc -l | awk '{ print$1 }')
 echo "$Display"


 
